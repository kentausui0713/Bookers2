<h2>Book detail</h2>
<table class="table">
  <tbody>
    <tr>
      <td>プロフィール画像</td>
      <td><%= link_to @book.title, book_path(@book) %></td>
      <td><%= @book.body %></td>
      <td><%= link_to 'edit', edit_book_path(@book) %></td>
      <td><%= link_to 'delete', book_path(@book), method: :delete, "data-confirm" => "本当に削除しますか？"%></td>
    </tr>
  </tbody>
</table>